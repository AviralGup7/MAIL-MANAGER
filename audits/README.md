# Audits

This directory once held a point-in-time audit per improvement wave — 01
through 58, plus four cross-audit consolidations. They were working
documents: each drove a round of fixes, and the fixes shipped. Their lasting
content lives in the code (every rule cites its round) and in the tests that
pin it; the documents themselves were retired when keeping them started
costing more attention than they returned (round 65 cleanup).

Two audits survive, because each is still load-bearing:

| File | Why it stays |
|---|---|
| `08-GMAIL-COMPETITIVE-V2.md` | The specification home for deliberately unbuilt features. `src/app/settings.js` refuses to carry a schema entry for `autoSyncMinutes` precisely because it is only *specified here* — deleting this file would orphan that reference and lose the spec. |
| `64-COMPREHENSIVE-RATING-V2.md` | The most recent whole-app rating. It is the baseline the round-65 interaction campaign (`docs/UX-AUDIT-V4.md`) measures itself against — the "before" in the before/after. |
| `RESPONSIVE-ADAPTIVE-ARCHITECTURE-AUDIT.md` | The adaptive-architecture reference: probe-measured breakpoint ladder and live-resize state evidence. All R-findings carry resolution commits (R-A1..R-A5 fixed, R-A6/R-A7 verified and closed). |
| `ACCESSIBILITY-INPUT-ARCHITECTURE-AUDIT.md` | The input/focus/semantics reference: AX-tree-proven census, the focus/overlay/announcement maps, and the ranked A-findings. All confirmed defects are fixed and pinned (A-A1 `bd5ee8c`, A-A2 `011c50b`, A-A3 `44cd0e2`, A-A6 `1f132a1`, A-A4/A-A7 `2304e5d`; A-A5 was withdrawn as an erratum). One debt remains: A-A9 — the body iframe's headless AX absence is control-proven a harness artifact, so a real NVDA/VoiceOver pass on real hardware is the only verdict path left. |
| `2026-08-15-SYSTEMWIDE-AUDIT.md` | The post-fuzz-sweep whole-system audit (2026-08-15): MV3 lifecycle, OAuth, sync, account isolation and storage economics verified against code at `2b82f0e`. Two CRITICAL findings (both the same root cause — the account is an assumption, not an identity): silent OAuth renewal follows the browser's current account with no profile validation (AUD-C1), and the outbox is neither account-scoped nor cleared at sign-out (AUD-C2). Phased roadmap P0–P3 inside; P0 is the only behavior change proposed. |
| `2026-08-15-SYSTEMWIDE-AUDIT-R2.md` | Round 2, same day, post-remediation re-audit at `ac0cbf2` (P0–P3 landed and CI-green). Verifies each round-1 fix against code rather than trust, second-witnesses the two parallel branch audits (`AUD2-*` upholds H1/H2/H3, downgrades L1), and adds the round's own reproduced findings: three forever-loops at the new identity fences (250 ms foreign-outbox pump loop R2-M1, 8-row pump-batch blockade R2-M2, unwakeable-snooze wake cadence R2-M3), the self-wiping tripwire scoreboard (R2-L1), and a sweep of LOWs. Roadmap R2-P0–P3; no production file modified. |
| `2026-08-15-INDEPENDENT-SYSTEMWIDE-AUDIT.md` | The deepest of the five same-day passes (audit of `ac0cbf2`): sequence-of-custody method. Found the two CRITICAL classes every other pass missed or acquitted — the background sweep consumes the one history cursor without durably applying it (AUD-I01) and the foreground cursor commits before durable apply (AUD-I02) — plus the non-idempotent-retry (AUD-I03), timeout-replay (AUD-I04) and identity-fail-open (AUD-I05) cluster. Rating 5.6/10. Canonical severity dispositions live in the reconciliation below. |
| `2026-08-15-EXTERNAL-SYSTEMWIDE-AUDIT.md` | The broadest of the five passes (audit of `ac0cbf2`): 20-way scored verification, 8.2/10. Found the worker OUTBOX_PUMP lost-update race both reports before it missed (EXT-H1: an enqueue during a pump is silently dropped; the reverse race resurrects a sent row), the string-typed error taxonomy (EXT-M2), and confirmed the coverage gate is unenforced and red (EXT-M3). Its "sync exemplary" acquittal is corrected in the reconciliation. |
| `2026-08-15-RECONCILIATION.md` | **The canonical register.** All five same-day audits adjudicated against the code: every disagreement resolved with evidence (including two self-reported errata), 8 root causes, 5 missed systemic interactions (MI-1..MI-5), the canonical finding set CAN-C1..I6 with severities, and a decision-complete roadmap (P0 truth/safety → P1 custody → P2 contracts/fences → P3 hygiene → P4 deferred). New findings attach here; this is the document to amend. |
| `2026-08-15-UI-INNOVATION-CYBERPUNK-REFERENCE-AUDIT.md` | Comprehensive UI innovation and Cyberpunk reference study: five downloaded Cyberpunk 2077 interface screenshots analyzed without copying assets, current skin compared against the reference grammar, 92 general UI opportunities, 96 Cyberpunk-specific improvements, phased roadmap and a generated file-by-file lens for every UI-facing or UI-adjacent source volume. |
| `2026-08-15-POST-IMPLEMENTATION-UI-DEFECT-AUDIT.md` | Deep post-implementation audit at `9c5ce59`: 10 HIGH, 20 MEDIUM and 14 LOW UI findings, including row pseudo-element collisions, incomplete Legacy/Calm contracts, false OAuth/notification copy, overlay-only search rendering, nested options fieldsets, missing Cyberpunk visual baselines, audio edge cases, modularization priorities and a computed-style/browser test roadmap. |
| `2026-08-15-ARCHITECTURE-FILE-ORGANIZATION-AUDIT.md` | Full architecture, placement and naming review: 114-module/304-edge graph, layer-boundary findings, file/test/CSS naming standard, documentation-link census, root-artifact decisions and implementation dispositions for feature ownership, renamed tests/tools, CSS volume splits and extraction candidates. |
| `ANIMATION-INTERACTION-AUDIT.md` | The motion overhaul's steering document and register: the measured census of the old stock, the surface→tier map, the binding spring/depth/lighting vocabulary, and the milestone plan — **P1–P7 all shipped** (springs → microtier → camera/overlays → signature morphs → key light/particles/breath), each commit-pinned, 218 local contracts green, live CDP walk 9/9. Integration runs on CI. |

Everything else was deleted on 2026-08-13 as history that could be
reconstructed from git if ever needed. The docs that govern how the app
works today live in [`../docs/`](../docs/).
